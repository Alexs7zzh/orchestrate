# Orchestrate repo guide

Read and follow [AGENTS.md](AGENTS.md), the provider-neutral contributor guide for this repository.
In particular, the authoritative minimum Herdr version is `MINIMUM_HERDR_VERSION` in
`scripts/src/herdr-contract.ts`, not its import site in `herdr-surface.ts`.
